# Protocol

The sub-pages of this chapter are generated by `mdbook-protobuf` from the `.proto` sources in the demo application.

For example, the [Greeter service](proto!(Greeter)) is defined at `demo/proto/helloworld.proto` with the 
following content:

```protobuf
{{#include ../../proto/helloworld.proto}}
```
